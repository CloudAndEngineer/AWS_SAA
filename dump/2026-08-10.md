# 2026-08-10 dump

41. 회사의 애플리케이션은 데이터 수집을 위해 여러 소프트웨어 즉 서비스(SaaS) 소스와 통합됩니다. 회사는 Amazon EC2 인스턴스를 실행하여 데이터를
수신하고 분석을 위해 Amazon S3 버킷에 데이터를 업로드합니다. 데이터를 수신하고 업로드하는 동일한 EC2 인스턴스는 업로드가 완료되면 사용자에
게 알림을 보냅니다. 회사는 애플리케이션 성능이 느린 것을 알아차리고 가능한 한 성능을 개선하고자 합니다.
어떤 솔루션이 최소한의 운영 오버헤드로 이러한 요구 사항을 충족할까요?

  * 현재 EC2가 데이터 수집, 업로드, 사용자 알림을 모두 담당하고 있어 애플리케이션 서버에 과도한 부하가 발생하고 있다.

  * B: Amazon AppFlow는 AWS 서비스 간에 코딩 없이 데이터를 안전하게 전송할 수 있는 완전관리형 API 통합 서비스이다. 기존에 EC2가 수행하던 작업을 Appfloe에 위임해서 소프트웨어에서 S3로 데이터를 전송하는 속도를 개선할 수 있다.

  * C: EventBridge는 이벤트 기반 애플리케이션 통합에는 적합하지만, SaaS 데이터를 직접 S3로 수집·전송하는 용도로는 AppFlow가 더 적합하다.


42. 한 회사가 단일 VPC의 Amazon EC2 인스턴스에서 고가용성 이미지 처리 애플리케이션을 실행합니다. EC2 인스턴스는 여러 가용성 영역에 걸쳐 여러
서브넷 내에서 실행됩니다. EC2 인스턴스는 서로 통신하지 않습니다. 그러나 EC2 인스턴스는 Amazon S3에서 이미지를 다운로드하고 단일 NAT 게이
트웨이를 통해 Amazon S3에 이미지를 업로드합니다. 이 회사는 데이터 전송 요금에 대해 우려하고 있습니다.
이 회사가 지역 데이터 전송 요금을 피하는 가장 비용 효율적인 방법은 무엇입니까?

  * C: S3에 Gateway VPC Endpoint를 사용하면 인터넷 연결 없이 내부망을 이용해 EC2와 통신할 수 있다. Gateway VPC Endpoint는 전면 무료이다.


43. 한 회사에는 Amazon S3에 백업되는 대량의 시간 민감한 데이터를 생성하는 온프레미스 애플리케이션이 있습니다. 애플리케이션이 커졌고 인터넷 대역
폭 제한에 대한 사용자 불만이 있습니다. 솔루션 아키텍트는 Amazon S3에 대한 적시 백업과 내부 사용자의 인터넷 연결에 미치는 영향을 최소화하는
장기 솔루션을 설계해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족합니까?

  * B: AWS Direct Connect를 이용해 내부 사용자의 인터넷 연결에 미치는 영향을 최소화하는 장기 솔루션을 설계할 수 있다.


44. 한 회사에 중요한 데이터가 들어 있는 Amazon S3 버킷이 있습니다. 회사는 실수로 데이터가 삭제되는 것을 방지해야 합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 어떤 단계 조합을 취해야 합니까? (두 가지를 선택하세요.)

  * A, B: 버전 관리를 사용하면 실수로 데이터를 삭제하더라고 이전 버전에 남아있어 복원할 수 있다. MFA로 중요한 작업에 추가 인증을 요구해 실수로 데이터를 삭제하는 것을 방지할 수 있다.

  * C: `s3:DeleteObject` 권한을 주지 않는 방식으로 삭제를 제한할 수 있지만, 대표적인 S3 기능 조합은 MFA + Versioning이다.


45. 한 회사에는 다음으로 구성된 데이터 수집 워크플로가 있습니다.
  * 새 데이터 전송에 대한 알림을 위한 Amazon Simple Notibcation Service(Amazon SNS) 주제
  * 데이터를 처리하고 메타데이터를 기록하는 AWS Lambda 함수
이 회사는 네트워크 연결 문제로 인해 수집 워크플로가 가끔 실패하는 것을 관찰합니다. 이러한 실패가 발생하면 Lambda 함수는 회사에서 수동으로 작
업을 다시 실행하지 않는 한 해당 데이터를 수집하지 않습니다.
솔루션 아키텍트는 Lambda 함수가 앞으로 모든 데이터를 수집하도록 하기 위해 어떤 작업 조합을 취해야 합니까? (두 가지를 선택하세요.)

  * B, E: SQS의 큐에 적재된 작업은 성공하면 큐에서 제거되고, 실패하면 큐에 남아있는다. SNS -> SQS -> Lambda의 순서대로 처리하도록 변경하면 실패 작업을 수동으로 재처리하지 않아도 된다.


46. 한 회사에 매장에 마케팅 서비스를 제공하는 애플리케이션이 있습니다. 이 서비스는 매장 고객의 이전 구매 내역을 기반으로 합니다. 매장은 SFTP를 통
해 거래 데이터를 회사에 업로드하고, 이 데이터는 처리 및 분석되어 새로운 마케팅 제안을 생성합니다. 일부 파일은 크기가 200GB를 초과할 수 있습니
다.
최근 이 회사는 일부 매장에서 포함되어서는 안 될 개인 식별 정보(PII)가 포함된 파일을 업로드했다는 사실을 발견했습니다. 이 회사는 PII가 다시 공유
되면 관리자에게 알림을 보내고 싶어합니다. 이 회사는 또한 수정을 자동화하려고 합니다.
솔루션 아키텍트는 최소한의 개발 노력으로 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?

  * B: Amazon Macie는 인공지능(AI)과 기계 학습(ML) 기술을 사용하여 클라우드 내 민감한 데이터를 자동으로 검색, 분류 및 보호하는 AWS의 완전 관리형 데이터 보안 및 데이터 프라이버시 서비스이다.


47. 회사에서는 1주일 동안 지속되는 다가올 이벤트를 위해 특정 AWS 지역의 3개 특정 가용성 영역에서 보장된 Amazon EC2 용량이 필요합니다.
회사는 EC2 용량을 보장하기 위해 무엇을 해야 합니까?

  * 예약 인스턴스는 장기간 계약을 요한다. 단기간 지속되는 이벤트에는 주문형 용량 예약이 적합하다. 가용성 영역까지 지정해야 하므로 D가 적절하다.


48. 한 회사의 웹사이트는 항목 카탈로그에 Amazon EC2 인스턴스 스토어를 사용합니다. 이 회사는 카탈로그가 고가용성이고 내구성 있는 위치에 저장되도
록 하려고 합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?

  * A: ElastiCache는 캐싱용으로는 적합하지만, 내구성에는 적합하지 않다.

  * D: EFS는 여러 EC2 Instance에서 접근 가능하고 내구성이 Instance Store보다 뛰어나다.


49. 한 회사가 월별로 통화 기록 파일을 저장합니다. 사용자는 통화 후 1년 이내에 무작위로 파일에 액세스하지만, 1년 후에는 드물게 파일에 액세스합니다.
이 회사는 사용자에게 1년 미만의 파일을 가능한 한 빨리 쿼리하고 검색할 수 있는 기능을 제공하여 솔루션을 최적화하고자 합니다. 이전 파일을 검색하
는 데 지연이 있어도 괜찮습니다.
어떤 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족할까요?

  * A: Glacier Instant Retrieval은 거의 즉시 접근이 가능하지만, 문제의 핵심은 1년 이후 접근 빈도가 낮아지는 데이터를 비용 효율적으로 보관하는 것이므로 적절하지 않다. 또한 Glacier 객체의 태그를 쿼리해서 파일을 검색하는 방식도 적절하지 않다.

  * B: 접근 패턴을 정확히 예측하기 어렵기 때문에 Intelligent-Tiering이 적합하다. 1년 후에는 파일 검색에 지연이 있어도 된다고 했으므로 Flexible Retrival은 적절한 선택지이다. 또한 S3에 저장된 데이터를 SQL로 직접 쿼리해야 한다면 Amazon Athena가 적합하다.

  * C: 1년 이후에도 Glacier Instant Retrieval을 사용하는 것은 비용 측면에서 비효율적이다.

  * D: 별도의 Amazon RDS에 검색 메타데이터를 저장하고 관리해야 하므로 운영 복잡성이 증가한다.


50. 한 회사에 1,000개의 Amazon EC2 Linux 인스턴스에서 실행되는 프로덕션 워크로드가 있습니다. 워크로드는 타사 소프트웨어로 구동됩니다. 이 회사는
중요한 보안 취약성을 해결하기 위해 가능한 한 빨리 모든 EC2 인스턴스에서 타사 소프트웨어를 패치해야 합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 합니까?

  * A: Lambda를 사용하면 직접 패치 로직을 개발하고 관리해야 하므로 운영 오버헤드가 크다.

  * B: AWS Systems Manager Patch Manager는 EC2 인스턴스의 OS 및 지원되는 애플리케이션에 대한 패치 관리 및 보안 업데이트를 중앙에서 자동화할 수 있는 서비스이다.

  * C: Maintenance Windows는 패치를 수행하는 기능 자체가 아니라 작업을 실행할 시간대를 예약하는 기능이다.

  * D: Run Command는 여러 EC2 인스턴스에서 명령을 원격으로 실행할 수 있는 서비스이다. 구현은 가능하지만 관리 로직을 직접 구성해야 하므로 운영 오버헤드가 크다.


51. 한 회사가 REST API에서 검색할 수 있는 주문 배송 통계를 제공하는 애플리케이션을 개발하고 있습니다. 이 회사는 배송 통계를 추출하고, 데이터를 읽
기 쉬운 HTML 형식으로 구성하고, 매일 아침 여러 이메일 주소로 동시에 보고서를 보내려고 합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 어떤 단계 조합을 취해야 합니까? (두 가지를 선택하세요.)

  * D + B: 매일 아침에 실행해야 하기 때문에 EventBridge를 사용하고 Lambda가 애플리케이션의 REST API를 호출하여 배송 통계를 가져온다. 그리고 SES를 사용하여 이메일로 전송한다.

  * A: Kinesis Data Firehose는 실시간/스트리밍 데이터를 목적지로 전달하는 서비스이므로 매일 아침에 실행하는 작업에 사용하기에는 적합하지 않다.

  * C: Glue는 주로 ETL 및 데이터 변환/카탈로그 작업에 사용된다(Raw Data -> Structured Data). 단순히 REST API에 사용하기에는 과도하다.


52. 한 회사가 온프레미스 애플리케이션을 AWS로 마이그레이션하려고 합니다. 이 애플리케이션은 수십 기가바이트에서 수백 테라바이트에 이르는 다양한
크기의 출력 파일을 생성합니다. 애플리케이션 데이터는 표준 파일 시스템 구조에 저장해야 합니다. 이 회사는 자동으로 확장되는 솔루션을 원합니다.
가용성이 높고 최소한의 운영 오버헤드만 필요합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?

  * A: S3는 객체 스토리지이므로 표준 파일 시스템 구조가 아니다.

  * B, D: EBS는 여러 인스턴스가 공유할 수 없다. 또한 EKS는 요구사항에 비해 과도하게 복잡하다.

  * C: EFS는 용량을 미리 프로비저닝할 필요 없이 데이터 증가에 따라 자동으로 확장된다. 적절한 선택지.


53. 어떤 회사는 Amazon S3에 회계 기록을 저장해야 합니다. 기록은 1년 동안 즉시 액세스할 수 있어야 하며, 그 후 9년 동안 보관해야 합니다. 관리자와 루
트 사용자를 포함하여 회사의 어느 누구도 10년 동안 기록을 삭제할 수 없습니다. 기록은 최대한의 복원력으로 저장해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?

  * A: 최근 1년 간 기록에 즉시 액세스할 수 없다.

  * B: 액세스 빈도가 불규칙적이라는 요구사항이 없으므로 Intelligent-Tiering은 비효율적이다. IAM 정책을 수동으로 설정하면 휴먼에러의 위험이 발생한다.

  * C: S3 Object Lock의 Compliance Mode를 사용하여 누구도 객체를 삭제할 수 없게 하였으므로 적절하다.

  * D: One-Zone IA는 복원력이 상대적으로 떨어진다. 또한 Governance mode에서는 특정 권한을 가진 사용자가 Object Lock 설정을 우회하여 삭제할 수 있다.
