# 2026-08-08 dump

17. 한 회사가 새로운 비즈니스 애플리케이션을 구현하고 있습니다. 이 애플리케이션은 두 개의 Amazon EC2 인스턴스에서 실행되고 문서 저장을 위해
Amazon S3 버킷을 사용합니다. 솔루션 아키텍트는 EC2 인스턴스가 S3 버킷에 액세스할 수 있는지 확인해야 합니다.
솔루션 아키텍트는 이 요구 사항을 충족하기 위해 무엇을 해야 합니까?

  * A: Role에 `Allow: s3: GetObject s3: PutObject`와 같은 정책을 연결할 수 있다. 그리고 이 Role을 EC2의 IAM Instance Profile에 연결한다.

  * B: Policy 자체를 EC2에 연결하는 방식은 사용할 수 없다.

  * C: IAM Group은 사용자를 그룹화하기 위한 기능이지, 인스턴스를 그룹화하는 기능이 아니다.

  * D: EC2 Instance에 IAM User를 연결하는 방식은 사용하지 않는다.


18. 애플리케이션 개발팀은 큰 이미지를 더 작고 압축된 이미지로 변환하는 마이크로서비스를 설계하고 있습니다. 사용자가 웹 인터페이스를 통해 이미지
를 업로드하면 마이크로서비스는 이미지를 Amazon S3 버킷에 저장하고, AWS Lambda 함수로 이미지를 처리하고 압축하고, 압축된 형태로 이미지를
다른 S3 버킷에 저장해야 합니다.
솔루션 아키텍트는 내구성이 뛰어나고 상태 없는 구성 요소를 사용하여 이미지를 자동으로 처리하는 솔루션을 설계해야 합니다.
이러한 요구 사항을 충족하는 작업의 조합은 무엇입니까? (두 가지를 선택하십시오.)

  * A, B: 사용자가 이미지 업로드 -> SQS에 알림 보내기 -> Lambda가 SQS를 호출 소스로 사용함 -> Lambda를 이용해 이미지 처리 -> Lambda가 메시지를 처리하면 SQS에서 메시지 삭제(Lambda가 처리에 실패하면 삭제되지 않음)

  * C: 파일 이름을 이름을 메모리의 텍스트 파일에 기록하면 Stateless 요구사항을 만족할 수 없음

  * D: 중간에 EC2를 둘 이유가 없음

  * E: 이메일을 전송하면 수신자가 수동으로 처리하는 구조이다. Lambda를 이용한 자동화가 이루어지지 않았다.


19. 한 회사에 AWS에 배포된 3계층 웹 애플리케이션이 있습니다. 웹 서버는 VPC의 퍼블릭 서브넷에 배포됩니다. 애플리케이션 서버와 데이터베이스 서버
는 동일한 VPC의 프라이빗 서브넷에 배포됩니다. 이 회사는 검사 VPC에 AWS Marketplace의 타사 가상 방화벽 어플라이언스를 배포했습니다. 이 어플
라이언스는 IP 패킷을 허용할 수 있는 IP 인터페이스로 구성되어 있습니다.
솔루션 아키텍트는 트래픽이 웹 서버에 도달하기 전에 애플리케이션에 대한 모든 트래픽을 검사하기 위해 웹 애플리케이션을 어플라이언스와 통합해야
합니다.
어떤 솔루션이 최소한의 운영 오버헤드로 이러한 요구 사항을 충족할까요?

  * 요구 사항을 만족하는 솔루션은 GWLB(Gateway Load Balancer) 사용이다.

  * A: NLB는 트래픽 검사 기능을 수행하지 않는다.

  * B: ALB는 타사 방화벽 어플라이언스를 네트워크 트래픽 검사 장비로 투명하게 삽입하는 용도로 적합하지 않다.

  * C: Transit Gateway는 네트워크 허브이지, 패킷을 검사하는 기능을 제공하지는 않는다.

  * D: GWLB를 이용한 적절한 솔루션이다.


20. 한 회사가 동일한 AWS 지역의 테스트 환경에 대량의 프로덕션 데이터를 복제하는 기능을 개선하고자 합니다. 데이터는 Amazon Elastic Block
Store(Amazon EBS) 볼륨의 Amazon EC2 인스턴스에 저장됩니다. 복제된 데이터에 대한 수정 사항은 프로덕션 환경에 영향을 미치지 않아야 합니다.
이 데이터에 액세스하는 소프트웨어는 지속적으로 높은 I/O 성능이 필요합니다.
솔루션 아키텍트는 프로덕션 데이터를 테스트 환경에 복제하는 데 필요한 시간을 최소화해야 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?

  * A: Instance Store는 EC2 호스트에 물리적으로 연결된 임시 스토리지이다. EC2 인스턴스가 종료되는 등의 상황에서는 데이터가 유실될 수 있다. 또한 EBS Snapshot을 Instance Store로 복원하는 방식도 적절하지 않다.

  * B: Multi-Attach는 하나의 EBS 볼륨을 여러 EC2 Instance에 연결하는 기능이므로 문제 상황에는 적절하지 않다.

  * C: 데이터 독립성은 확보되나, 복원 초기에 지연 시간이 발생해 성능이 저하된다.

  * D: 높은 I/O 성능을 만족하기 위해 Fast Snapshot Restore 기능을 적절하게 활용하였다.


21. 전자상거래 회사가 AWS에서 하루 한 건의 거래 웹사이트를 시작하려고 합니다. 매일 24시간 동안 정확히 한 가지 상품만 판매합니다. 이 회사는 피크 시
간대에 밀리초 지연 시간으로 매시간 수백만 건의 요청을 처리할 수 있기를 원합니다.
어떤 솔루션이 가장 적은 운영 오버헤드로 이러한 요구 사항을 충족할까요?

  * A: 정적 웹사이트를 S3에 호스팅하고 CloudFront를 사용하는 것은 좋으나, 주문 데이터를 S3에 저장하는 것은 적절하지 않다.

  * B: 서버리스 구조인 D보다 구조가 복잡하다.

  * C: 단순히 대규모 요청을 처리하기 위해 EKS를 사용하는 것은 과도하다.

  * D: 정적 웹사이트는 S3에 호스팅하고, 서버리스 기반으로 대규모 요청을 자동 처리할 수 있다. 운영 오버헤드 최소화 측면에서 서버리스가 적합하다.


22. 솔루션 아키텍트는 Amazon S3를 사용하여 새로운 디지털 미디어 애플리케이션의 스토리지 아키텍처를 설계하고 있습니다. 미디어 파일은 가용성 영역
의 손실에 대한 복원력이 있어야 합니다. 일부 파일은 자주 액세스되는 반면 다른 파일은 예측할 수 없는 패턴으로 거의 액세스되지 않습니다. 솔루션 아
키텍트는 미디어 파일을 저장하고 검색하는 비용을 최소화해야 합니다.
어떤 스토리지 옵션이 이러한 요구 사항을 충족합니까?

  * B: 예축할 수 없는 패턴으로 액세스되는 데이터에 대응하기 위해 지능형 계층화를 사용하는 것이 좋다.


23. 한 회사가 Amazon S3 Standard 스토리지를 사용하여 백업 파일을 저장하고 있습니다. 파일은 1개월 동안 자주 액세스됩니다. 그러나 1개월 후에는 파
일에 액세스하지 않습니다. 회사는 파일을 무기한 보관해야 합니다.
어떤 스토리지 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족할까요?

  * A: 1개월 이후에는 파일에 액세스하지 않으므로 Intelligent-Tiering은 적합하지 않다.

  * B: 액세스하지 않으면서 무기한으로 파일을 보관할 때는 Deep Archive가 적합하다.

  * C, D: B보다 비효율적인 선택지이다.
