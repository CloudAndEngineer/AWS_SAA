# 2026-08-11 dump

54. 한 회사가 AWS에서 여러 Windows 워크로드를 실행합니다. 이 회사의 직원은 두 개의 Amazon EC2 인스턴스에 호스팅된 Windows 파일 공유를 사용합
니다. 이 파일 공유는 서로 간에 데이터를 동기화하고 중복된 사본을 유지합니다. 이 회사는 사용자가 현재 파일에 액세스하는 방식을 보존하는 고가용
성 및 내구성 있는 스토리지 솔루션을 원합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 합니까?

  * A: 기존 사용자는 Windows 파일 공유를 사용하고 있는데 S3로 옮기면 접근 방식이 달라진다.

  * B: S3 File Gateway는 온프레미스 파일 환경에서 S3를 파일 저장소처럼 사용할 때 유용하다. AWS에 이미 존재하는 Windows 파일 공유 환경을 고가용성 Windows 파일 시스템으로 교체하려는 상황에는 적절하지 않다.

  * C: FSx for Windows File Server 역시 SMB 프로토콜을 지원하므로 기존 사용자들의 파일 접근 방식을 거의 그대로 유지할 수 있다. 적절한 선택지.

  * D: EFS는 Linux 기반 워크로드의 NFS 파일 시스템에 적합하다.


55. 솔루션 아키텍트는 여러 서브넷을 포함하는 VPC 아키텍처를 개발하고 있습니다. 이 아키텍처는 Amazon EC2 인스턴스와 Amazon RDS DB 인스턴스를
사용하는 애플리케이션을 호스팅합니다. 이 아키텍처는 두 개의 가용성 영역에 있는 여섯 개의 서브넷으로 구성됩니다. 각 가용성 영역에는 퍼블릭 서브
넷, 프라이빗 서브넷, 데이터베이스 전용 서브넷이 포함됩니다. 프라이빗 서브넷에서 실행되는 EC2 인스턴스만 RDS 데이터베이스에 액세스할 수 있습
니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?

  * A: Route Table은 트래픽의 경로를 결정한다. 접근 제어를 담당하는 서비스가 아니다.

  * B: 보안 그룹에서는 Deny 규칙을 설정할 수 없다.

  * C: AWS Security Group에서는 IP 주소 대신 다른 Security Group을 source로 참조할 수 있다. 이렇게 하면 해당 보안 그룹을 사용하고 있는 인스턴스만 Allow할 수 있으므로 적절한 방법이다.

  * D: VPC Peering은 서로 다른 VPC 간 네트워크 연결을 위한 기능이다.


56. 한 회사가 Amazon Route 53에 도메인 이름을 등록했습니다. 이 회사는 ca-central-1 지역의 Amazon API Gateway를 백엔드 마이크로서비스 API의 퍼
블릭 인터페이스로 사용합니다. 타사 서비스는 API를 안전하게 사용합니다. 이 회사는 타사 서비스가 HTTPS를 사용할 수 있도록 회사의 도메인 이름과
해당 인증서로 API Gateway URL을 설계하려고 합니다.
어떤 솔루션이 이러한 요구 사항을 충족할까요?

  * A: Stage Variable은 API Gateway의 기본 URL 자체를 사용자 지정 도메인으로 변경하는 기능이 아니다. 또한 HTTPS 인증서와 Custom Domain 설정도 해결하지 못한다.

  * B, D: API Gateway Regional endpoint의 Custom Domain Name 인증서는 API Gateway endpoint와 동일한 리전에 있어야 한다.

  * C: Regional API Gateway endpoint 생성 -> Custom Domain Name 설정 -> ACM 인증서 생성/가져오기 -> Route 53 설정


57. 한 회사가 인기 있는 소셜 미디어 웹사이트를 운영하고 있습니다. 이 웹사이트는 사용자에게 이미지를 업로드하여 다른 사용자와 공유할 수 있는 기능을
제공합니다. 이 회사는 이미지에 부적절한 콘텐츠가 포함되지 않도록 하려고 합니다. 이 회사는 개발 노력을 최소화하는 솔루션이 필요합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?

  * A: Amazon Comprehend는 자연어 처리(NLP) 서비스이다.

  * B: Amazon Rekognition은 이미지와 비디오를 분석하는 완전관리형 AI 서비스이다. 특히 DetectModerationLabels API를 사용하면 이미지에 다음과 같은 부적절한 콘텐츠가 포함되어 있는지 자동으로 탐지할 수 있다. 적절한 선택지.

  * C: SageMaker는 직접 ML 모델을 구축하고 학습/배포해야 하는 경우에 적합하다.

  * D: 많은 개발 및 운영 작업이 필요하다. 이미 Amazon Rekognition이 관련 기능을 제공하므로 모델을 따로 개발할 필요가 없다.


58. 어떤 회사는 확장성과 가용성에 대한 요구 사항을 충족하기 위해 컨테이너에서 중요한 애플리케이션을 실행하고자 합니다. 이 회사는 중요한 애플리케
이션의 유지 관리에 집중하는 것을 선호합니다. 이 회사는 컨테이너화된 워크로드를 실행하는 기본 인프라를 프로비저닝하고 관리하는 책임을 맡고 싶
어하지 않습니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?


  * C: AWS Fargate는 가상 머신(EC2)의 인프라를 직접 관리할 필요 없이 컨테이너를 실행할 수 있게 해주는 서버리스 컴퓨팅 엔진이다. 기본 인프라를 프로비저닝할 필요가 없으므로 적절한 선택지이다.


59. 한 회사가 300개가 넘는 글로벌 웹사이트와 애플리케이션을 호스팅합니다. 이 회사는 매일 30TB가 넘는 클릭스트림 데이터를 분석할 플랫폼이 필요합
니다.
솔루션 아키텍트는 클릭스트림 데이터를 전송하고 처리하기 위해 무엇을 해야 합니까?

  * A: AWS Data Pipeline은 데이터 이동 및 처리 파이프라인을 구축할 수 있지만, 이 문제의 핵심인 대규모 클릭스트림 스트리밍 데이터 수집에는 Kinesis가 더 적합하다. 또한 EMR 클러스터를 직접 운영해야 하므로 운영 오버헤드가 증가한다.

  * D: Kinesis Data Streams가 대규모 데이터 수집 -> Kinesis Data Firehose가 스트리밍 데이터를 자동으로 전달 -> S3를 이용해 대규모 데이터를 저렴하게 저장할 수 있는 Data Lake로 활용 -> Amazon Redshift가 S3에 저장된 대규모 데이터를 SQL 기반으로 분석. 적절한 선택지.


60. 한 회사에 AWS에 호스팅된 웹사이트가 있습니다. 이 웹사이트는 HTTP와 HTTPS를 별도로 처리하도록 구성된 Application Load Balancer(ALB) 뒤에
있습니다. 이 회사는 모든 요청을 웹사이트로 전달하여 요청이 HTTPS를 사용하도록 하려고 합니다.
이 요구 사항을 충족하기 위해 솔루션 아키텍트는 무엇을 해야 할까요?

  * A: NACL을 보기와 같이 업데이트하면 HTTP 요청이 차단되며, 리디렉션을 할 수는 없다.

  * C: ALB에서는 Listener Rule의 Redirect action을 사용할 수 있다. HTTP 트래픽을 HTTPS로 리디렉션할 수 있다.

  * D: ALB를 NLB로 교체하는 것과는 관련이 없다. HTTP/HTTPS는 7계층 프로토콜이므로 NLB로 HTTP 프로토콜을 리디렉션할 수 없다.


61. 한 회사가 AWS에서 2계층 웹 애플리케이션을 개발하고 있습니다. 이 회사의 개발자는 백엔드 Amazon RDS 데이터베이스에 직접 연결되는 Amazon
EC2 인스턴스에 애플리케이션을 배포했습니다. 이 회사는 애플리케이션에 데이터베이스 자격 증명을 하드코딩해서는 안 됩니다. 또한 이 회사는 정기
적으로 데이터베이스 자격 증명을 자동으로 순환하는 솔루션을 구현해야 합니다.
어떤 솔루션이 이러한 요구 사항을 가장 적은 운영 오버헤드로 충족할까요?

  * A: Instance Metadata는 EC2 인스턴스의 메타데이터를 조회하기 위한 기능이지 DB 비밀번호를 안전하게 관리하고 자동으로 로테이션하는 Secret 저장소가 아니다.

  * C: Secrets Manager에 자격 증명을 저장하고 자동 로테이션을 돌릴 수 있다.

  * D: Systems Manager Parameter Store는 자동 로테이션 기능을 제공하지 않는다.


62. 한 회사가 AWS에 새로운 퍼블릭 웹 애플리케이션을 배포하고 있습니다. 애플리케이션은 ALB(Application Load Balancer) 뒤에서 실행됩니다. 애플리
케이션은 외부 인증 기관(CA)에서 발급한 SSL/TLS 인증서로 에지에서 암호화되어야 합니다. 인증서는 만료되기 전에 매년 순환해야 합니다.
솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 합니까?

  * D: 외부 인증서는 AWS Certificate Manager(ACM)이 자동으로 갱신할 수 없다. 따라서 EventBridge를 이용해 알림을 보낸 뒤 수동으로 인증서를 갱신해야 한다.


63. 어떤 회사가 AWS에서 인프라를 운영하고 있으며 문서 관리 애플리케이션에 등록된 사용자 수가 700,000명입니다. 이 회사는 대용량 .pdf 파일을 .jpg
이미지 파일로 변환하는 제품을 만들려고 합니다. .pdf 파일의 평균 크기는 5MB입니다. 이 회사는 원본 파일과 변환된 파일을 저장해야 합니다. 솔루션
아키텍트는 시간이 지남에 따라 빠르게 증가할 수요를 수용할 수 있는 확장 가능한 솔루션을 설계해야 합니다.
어떤 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족합니까?

  * A: EC2 인스턴스나 EBS/EFS를 직접 관리할 필요가 없어 운영 오버헤드가 매우 낮다. 사용한 Lambda 실행 시간과 S3 저장공간 중심으로 비용이 발생하므로 지속적으로 EC2를 운영하는 것보다 비용 효율적이다.

  * C: 구현은 가능하나 서버리스인 A보다 운영 오버헤드가 더 크다.

  * D: EFS는 여러 EC2 인스턴스에서 공유해서 사용할 수 있는 파일 시스템이라는 장점은 있지만, 단순히 PDF와 JPG를 저장하고 변환하는 이 문제에서는 S3가 더 적합하다.
